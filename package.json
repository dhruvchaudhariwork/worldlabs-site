{
  "name": "worldlabs-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "World Labs — Expert gaming data for AI training and evaluation (projects in development).",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "demo": "node scripts/demo.js",
    "test": "node --test tests/*.test.js",
    "setup": "node scripts/setup.js"
  },
  "engines": {
    "node": ">=20"
  }
}
